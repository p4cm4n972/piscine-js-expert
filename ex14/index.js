/**
 * EX14 - Projet Final : Mini Framework Réactif
 *
 * Implémentez un mini-framework réactif avec :
 * - Système de réactivité (Proxy)
 * - Computed values
 * - Effects
 * - Virtual DOM basique
 * - System de composants
 */

// Votre code ici
// Vous avez carte blanche pour l'architecture !

// Suggestions de structure :
// - reactive(obj) : rend un objet réactif
// - ref(value) : crée une référence réactive
// - computed(fn) : valeur calculée
// - effect(fn) : side effect réactif
// - h(tag, props, children) : crée un vnode
// - mount(vnode, container) : monte dans le DOM

module.exports = {
    // Exportez vos fonctions ici
};
