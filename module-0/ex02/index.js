/**
 * Returns a function that greets with the given greeting
 * @param {string} greeting
 * @returns {Function}
 */
function createGreeter(greeting) {
    // Votre code ici (retournez une fonction)
}

/**
 * Sums all arguments
 * @param {...number} numbers
 * @returns {number}
 */
function sumAll(...numbers) {
    // Votre code ici (utilisez rest parameters)
}

/**
 * Applies an operation function to two numbers
 * @param {number} a
 * @param {number} b
 * @param {Function} operation
 * @returns {number}
 */
function applyOperation(a, b, operation) {
    // Votre code ici
}

/**
 * Returns a function that multiplies by factor
 * @param {number} factor
 * @returns {Function}
 */
function createMultiplier(factor) {
    // Votre code ici (closure)
}

module.exports = {
    createGreeter,
    sumAll,
    applyOperation,
    createMultiplier
};
