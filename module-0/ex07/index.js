/**
 * Returns grade letter based on score
 * @param {number} score
 * @returns {string} Grade (A, B, C, D, F)
 */
function getGrade(score) {
    // Votre code ici (utilisez if/else ou switch)
}

/**
 * Returns FizzBuzz array from 1 to n
 * @param {number} n
 * @returns {Array}
 */
function fizzBuzz(n) {
    // Votre code ici
}

/**
 * Sums all numbers from start to end (inclusive)
 * @param {number} start
 * @param {number} end
 * @returns {number}
 */
function sumRange(start, end) {
    // Votre code ici (utilisez une boucle)
}

/**
 * Counts occurrences of target in array
 * @param {Array} arr
 * @param {*} target
 * @returns {number}
 */
function countOccurrences(arr, target) {
    // Votre code ici (utilisez for...of)
}

/**
 * Returns array of object keys
 * @param {Object} obj
 * @returns {string[]}
 */
function getObjectKeys(obj) {
    // Votre code ici (utilisez for...in)
}

module.exports = {
    getGrade,
    fizzBuzz,
    sumRange,
    countOccurrences,
    getObjectKeys
};
