/**
 * Performs arithmetic operations
 * @param {number} a
 * @param {number} b
 * @param {string} operator - '+', '-', '*', '/', '%', '**'
 * @returns {number}
 */
function calculate(a, b, operator) {
    // Votre code ici
}

/**
 * Checks if value is in range [min, max]
 * @param {number} value
 * @param {number} min
 * @param {number} max
 * @returns {boolean}
 */
function isInRange(value, min, max) {
    // Votre code ici
}

/**
 * Returns the maximum of two numbers using ternary
 * @param {number} a
 * @param {number} b
 * @returns {number}
 */
function getMax(a, b) {
    // Votre code ici (utilisez le ternaire)
}

/**
 * Returns value or defaultValue if value is null/undefined
 * @param {*} value
 * @param {*} defaultValue
 * @returns {*}
 */
function nullishDefault(value, defaultValue) {
    // Votre code ici (utilisez ?? ou vérification manuelle)
}

module.exports = { calculate, isInRange, getMax, nullishDefault };
