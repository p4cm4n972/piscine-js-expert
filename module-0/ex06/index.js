/**
 * Reverses a string
 * @param {string} str
 * @returns {string}
 */
function reverseString(str) {
    // Votre code ici
}

/**
 * Checks if a string is a palindrome (case insensitive)
 * @param {string} str
 * @returns {boolean}
 */
function isPalindrome(str) {
    // Votre code ici
}

/**
 * Counts vowels in a string
 * @param {string} str
 * @returns {number}
 */
function countVowels(str) {
    // Votre code ici
}

/**
 * Capitalizes first letter of each word
 * @param {string} str
 * @returns {string}
 */
function capitalizeWords(str) {
    // Votre code ici
}

/**
 * Truncates string to maxLength and adds "..."
 * @param {string} str
 * @param {number} maxLength
 * @returns {string}
 */
function truncate(str, maxLength) {
    // Votre code ici
}

module.exports = {
    reverseString,
    isPalindrome,
    countVowels,
    capitalizeWords,
    truncate
};
