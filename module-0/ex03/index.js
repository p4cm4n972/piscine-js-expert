/**
 * Returns the last element of an array
 * @param {Array} arr
 * @returns {*}
 */
function getLastElement(arr) {
    // Votre code ici
}

/**
 * Adds an element to the end of array (immutably)
 * @param {Array} arr
 * @param {*} element
 * @returns {Array} New array
 */
function addToEnd(arr, element) {
    // Votre code ici (ne modifiez pas arr original!)
}

/**
 * Removes the first element (immutably)
 * @param {Array} arr
 * @returns {Array} New array
 */
function removeFirst(arr) {
    // Votre code ici
}

/**
 * Returns middle elements (without first and last)
 * @param {Array} arr
 * @returns {Array}
 */
function getMiddleElements(arr) {
    // Votre code ici (utilisez slice)
}

module.exports = {
    getLastElement,
    addToEnd,
    removeFirst,
    getMiddleElements
};
