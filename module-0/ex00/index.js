/**
 * Returns the type of a value
 * @param {*} value - Any value
 * @returns {string} Type of the value
 */
function getType(value) {
    // Votre code ici
    // Indice : typeof, mais attention aux cas spéciaux (null, array)
}

/**
 * Converts a value to a number
 * @param {*} value - Value to convert
 * @returns {number|NaN} Converted number or NaN
 */
function convertToNumber(value) {
    // Votre code ici
}

/**
 * Strict equality comparison (===)
 * @param {*} a - First value
 * @param {*} b - Second value
 * @returns {boolean} True if strictly equal
 */
function isStrictEqual(a, b) {
    // Votre code ici
}

/**
 * Loose equality comparison (==)
 * @param {*} a - First value
 * @param {*} b - Second value
 * @returns {boolean} True if loosely equal
 */
function isLooseEqual(a, b) {
    // Votre code ici
}

module.exports = {
    getType,
    convertToNumber,
    isStrictEqual,
    isLooseEqual
};
