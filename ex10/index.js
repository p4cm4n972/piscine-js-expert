function inherit(Child, Parent) {
    // Votre code ici
}

function getPrototypeChain(obj) {
    // Votre code ici
}

function deepClone(obj) {
    // Votre code ici
}

module.exports = { inherit, getPrototypeChain, deepClone };
