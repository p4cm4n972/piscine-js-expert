/**
 * Creates a counter with private state
 */
function createCounter(initial = 0) {
    // Votre code ici
}

/**
 * Creates a bank account with private balance
 */
function createBankAccount(initialBalance = 0) {
    // Votre code ici
}

/**
 * Creates a stack with private data
 */
function createStack() {
    // Votre code ici
}

/**
 * Creates an event emitter with private listeners
 */
function createEventEmitter() {
    // Votre code ici
}

module.exports = {
    createCounter,
    createBankAccount,
    createStack,
    createEventEmitter
};
